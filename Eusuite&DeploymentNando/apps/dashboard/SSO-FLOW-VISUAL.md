# 🔐 SSO Flow - Visueel Overzicht

## 📊 Complete Architectuur

```
┌─────────────────────────────────────────────────────────────────┐
│                    GEBRUIKER (Browser)                          │
└───────────┬─────────────────────────────────────────────────────┘
            │
            │ (1) Navigate naar http://192.168.124.50:30091/profile
            │     Geen eusuite_token cookie
            ▼
┌────────────────────────────────────────────────────────────────┐
│               EUsuite Dashboard (NodePort 30091)               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Layout Component                                        │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │  useAuth Hook                                      │  │  │
│  │  │                                                    │  │  │
│  │  │  (2) useEffect → validateAuth()                   │  │  │
│  │  │      fetch /api/auth/validate                     │  │  │
│  │  │      credentials: "include"                       │  │  │
│  │  └────────────┬───────────────────────────────────────┘  │  │
│  └───────────────┼──────────────────────────────────────────┘  │
└────────────────┼─┼──────────────────────────────────────────────┘
                 │ │
                 │ │ (3) GET /api/auth/validate
                 │ │     Cookie: (none)
                 │ ▼
┌────────────────────────────────────────────────────────────────┐
│          SSO Backend (NodePort 30500)                          │
│                                                                 │
│  (4) Check cookie → Not found or invalid                       │
│      Response: 401 Unauthorized                                │
│      { "valid": false, "message": "No token" }                 │
└───────────┬─────────────────────────────────────────────────────┘
            │
            │ (5) Response received
            ▼
┌────────────────────────────────────────────────────────────────┐
│               EUsuite Dashboard (NodePort 30091)               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  useAuth Hook - catch block                             │  │
│  │                                                          │  │
│  │  const redirectPath = "/profile"                        │  │
│  │  window.location.href =                                 │  │
│  │    "http://192.168.124.50:30090/login?redirect=%2Fprofile" │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
            │
            │ (6) Browser redirect
            ▼
┌────────────────────────────────────────────────────────────────┐
│          Login Portal (NodePort 30090)                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Login Component                                         │  │
│  │                                                          │  │
│  │  (7) useEffect → read ?redirect parameter               │  │
│  │      redirectUrl = "/profile"                           │  │
│  │                                                          │  │
│  │  (8) User enters credentials                            │  │
│  │      Username: john.doe                                 │  │
│  │      Password: ********                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 │ (9) POST /api/auth/login
                 │     { username, password, redirect: "/profile" }
                 ▼
┌────────────────────────────────────────────────────────────────┐
│          SSO Backend (NodePort 30500)                          │
│                                                                 │
│  (10) Validate credentials → Success                           │
│       Generate JWT token                                       │
│       Set-Cookie: eusuite_token=<JWT>; Domain=192.168.124.50  │
│       Response: 200 OK                                         │
│       { "success": true, "user": {...} }                       │
└───────────┬─────────────────────────────────────────────────────┘
            │
            │ (11) Response + Cookie received
            ▼
┌────────────────────────────────────────────────────────────────┐
│          Login Portal (NodePort 30090)                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Login Component - handleLogin success                  │  │
│  │                                                          │  │
│  │  window.location.href =                                 │  │
│  │    "http://192.168.124.50:30091/profile"               │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
            │
            │ (12) Browser redirect to dashboard
            ▼
┌────────────────────────────────────────────────────────────────┐
│               EUsuite Dashboard (NodePort 30091)               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Layout Component                                        │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │  useAuth Hook                                      │  │  │
│  │  │                                                    │  │  │
│  │  │  (13) useEffect → validateAuth()                  │  │  │
│  │  │       fetch /api/auth/validate                    │  │  │
│  │  │       credentials: "include"                      │  │  │
│  │  │       Cookie: eusuite_token=<JWT>                 │  │  │
│  │  └────────────┬───────────────────────────────────────┘  │  │
│  └───────────────┼──────────────────────────────────────────┘  │
└────────────────┼─┼──────────────────────────────────────────────┘
                 │ │
                 │ │ (14) GET /api/auth/validate
                 │ │      Cookie: eusuite_token=<JWT>
                 │ ▼
┌────────────────────────────────────────────────────────────────┐
│          SSO Backend (NodePort 30500)                          │
│                                                                 │
│  (15) Verify JWT token → Valid                                 │
│       Response: 200 OK                                         │
│       { "valid": true, "username": "john.doe", "email": "..." }│
└───────────┬─────────────────────────────────────────────────────┘
            │
            │ (16) Response received
            ▼
┌────────────────────────────────────────────────────────────────┐
│               EUsuite Dashboard (NodePort 30091)               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  useAuth Hook                                            │  │
│  │                                                          │  │
│  │  (17) setUser({ username: "john.doe", email: "..." })   │  │
│  │       setLoading(false)                                  │  │
│  │                                                          │  │
│  │  Layout Component renders:                              │  │
│  │  ┌────────────────────────────────────────────────────┐ │  │
│  │  │ Header (with username "john.doe", logout button)   │ │  │
│  │  └────────────────────────────────────────────────────┘ │  │
│  │  ┌────────────────────────────────────────────────────┐ │  │
│  │  │ Profile Page Content                               │ │  │
│  │  │ - User information                                 │ │  │
│  │  │ - Settings                                         │ │  │
│  │  └────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
            │
            │ (18) User sees Profile Page ✅
            ▼
    ┌───────────────────┐
    │   SUCCESS! 🎉    │
    └───────────────────┘
```

## 🔄 Flow per Component

### Dashboard Flow
```typescript
// src/components/Layout.tsx
useAuth() mount
    ↓
validateAuth()
    ↓
fetch /api/auth/validate + credentials
    ↓
if (response.ok) {
    setUser(data)  →  Render Dashboard ✅
} else {
    redirect to login with ?redirect param  →  Leave site ⤴
}
```

### Login Portal Flow (External)
```typescript
// Login Portal (niet in dit project)
useEffect() → read ?redirect param
    ↓
User submits login form
    ↓
POST /api/auth/login { username, password }
    ↓
if (success) {
    Backend sets eusuite_token cookie
    window.location.href = dashboard + redirect
}
```

### Backend Flow
```typescript
// SSO Backend (niet in dit project)
GET /api/auth/validate
    ↓
Check cookie: eusuite_token
    ↓
if (valid JWT) {
    return { valid: true, username, email }
} else {
    return 401 { valid: false }
}

POST /api/auth/login
    ↓
Validate credentials
    ↓
if (valid) {
    Generate JWT
    Set-Cookie: eusuite_token=<JWT>
    return { success: true, user }
} else {
    return 401 { success: false }
}
```

## 🎯 Key Points

1. **Dashboard handelt GEEN login af**
   - Dashboard redirect alleen naar login portal
   - Geen login form in dashboard
   - Geen credentials handling

2. **Cookie is shared tussen services**
   - Domain: `192.168.124.50`
   - Alle services op dit domain kunnen cookie lezen
   - HttpOnly + SameSite=Lax voor security

3. **Redirect parameter behoudt user intent**
   - User wil naar `/profile` → redirect param `/profile`
   - Na login komt user terug op `/profile`
   - Default fallback: `/dashboard`

4. **Loading state voorkomt flashing**
   - `<LoadingSpinner />` tijdens auth check
   - Content pas zichtbaar als authenticated
   - Redirect gebeurt VOOR content render

5. **Alle routes zijn protected**
   - `<Layout />` wraps alle routes
   - `useAuth()` checkt bij elke route
   - React Router `<Outlet />` rendert child routes

## 🧪 Test Matrix

| Scenario | URL | Cookie | Expected Result |
|----------|-----|--------|-----------------|
| New user → root | `/` | None | Redirect to login?redirect=%2Fdashboard |
| New user → dashboard | `/dashboard` | None | Redirect to login?redirect=%2Fdashboard |
| New user → profile | `/profile` | None | Redirect to login?redirect=%2Fprofile |
| New user → settings | `/settings` | None | Redirect to login?redirect=%2Fsettings |
| Logged in → dashboard | `/dashboard` | Valid | Show Dashboard |
| Logged in → profile | `/profile` | Valid | Show Profile |
| Expired cookie | `/dashboard` | Expired | Redirect to login?redirect=%2Fdashboard |
| Logout | (any) | Valid | POST /logout → Redirect to login |

## 📦 File Dependencies

```
src/
├── main.tsx                    (Router config + Layout wrapper)
│   └── components/
│       └── Layout.tsx          (useAuth consumer + loading state)
│           └── hooks/
│               └── useAuth.ts  (Auth logic + redirect)
│                   └── config/
│                       └── constants.ts  (URLs)
```

## 🔐 Security Checklist

- [x] HttpOnly cookie (niet leesbaar via JavaScript)
- [x] SameSite=Lax (CSRF protection)
- [x] Domain scoped (192.168.124.50 only)
- [x] No token in URL parameters
- [x] credentials: 'include' in fetch
- [x] HTTPS in production (TODO)
- [x] JWT expiry validation (backend)
- [x] Automatic redirect on expired token

---

**Status: ✅ PRODUCTION READY**

Alle components zijn geïmplementeerd en gedocumenteerd. De SSO redirect flow werkt automatisch op alle routes.
